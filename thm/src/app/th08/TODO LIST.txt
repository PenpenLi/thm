----
TODO LIST:
1:[-]场景转换,我估摸菜单和游戏是两个不同的场景,但是等待的加载画面就不得而知了
2:[-]模块设计,感觉有问题啊,因为不是每个模块都是实时打开的,是由那个控制打开,怎么控制打开?controller?
3:[-]实体这块需求有点不明,不知道要搞什么
4:[-]Stage这块很重要,但是各部分不知道怎么组织,有点乱,而且
5:[-]动画系统,控制系统怎么与Stage组织起来,
6:[-]任务系统,如果有循环的怎么办,时间回溯?,还是多搞几个一样的?

2018-12-24
1:[√](待议)PositionComponent与runAction()产生冲突,两者只能取一,出于ccNode已经包含必要的组件,所以Entity不可能只有数据,(这里只能说ECS.Entity耦合了cc.Node,导致耦合度过高,目前来看没有很好的解决方法)
2:[]Batman与Layer与Scene与ScheduleTask的关系
3:[]AnimationComponent的动画与CollisionComponent的矩形不一定需要一致,因为动画可能尺寸很长,但实际碰撞不需要符合尺寸
4:[-]组件这块事实上应该是可以带方法的,不然会很麻烦
5:[√]网格碰撞没弄好,不应该设置在组件上
6:[√]System应该负责一组组件的记录和遍历,比如CollisionComponent只有数据或方法,不做其他用途,如网格碰撞的检测
7:[-]对5,6这个网格碰撞的存在异议,实现方法有待研究
8:[]对单例组件的实现(如输入组件)
9:[√]组件可以获得其他组件,也可以设置
10:[√]Component是可以取到Entity的,Component应该包含getEntity()方法
11:[-](待议)组件顺序,脚本顺序,以及组件分类(有些组件只能有一个,有些是多个,如脚本组件,声音组件)    --这个可以用Map + List的组合,因为getComponent只取第一个,因此效率O(1),需要特殊处理的是脚本组件的查找(理论上无此函数,但为了子类能够访问父类脚本而设置),添加时遍历一次组件表以确认是否添加过(依据????)
12:[√]子弹分类-光玉:(小玉,大玉), 激光又可以根据不同类型细分

2018-12-25
1:[-]ECS,再次重构,Component只有数据没有方法,(尽量少或没有方法,尽量或没有关联其他组件),Entity需要分成CCEntity和Entity
构造时加入自管理,调用Destroy()时释放,(去掉实体回调)
2:---
3:[-]脚本与脚本之间传递消息
4:[-]Entity添加,搜索算法有问题
6:[√]用CCNode的注册的回调,所创建的对象无法超过10000个,需要重新写基类
7:[]TimeLine技术,能够被暂停,但时间依然流逝
8:[-]System监视Component,通过观察者模式通知System,这个组件被移除
9:[√]Component与System处理不太好,无法获得大类,无法知道组件消亡..

2019-01-02
1:[](严重)脚本优先级,Entity(内部之间的优先级)[Component(内部之间的优先级) > Script(内部之间的优先级)] > System(内部之间的优先级)
2:[√]实体消亡动画不应该写在onExit()中,实体Destroy() 不等于 "被杀死" 这种状态
3:[]需要一个实体创建的工厂创建实体
4:[]碰撞系统需要支持碰撞进入,碰撞中,碰撞退出,还需要触发器组件

2019-01-03
1:[√]需要一个粒子系统
2:[√]声音,动画,粒子这些全部属于资源
3:[√]动画组件,行为组件与资源之间的关系
4:[√]脚本,组件的继承,onClass()无法取得分类名字问题

2019-01-04
1:[√]输入轮询系统和组件
2:[-]对象缓存池,Entity的Destroy()会把所有脚本移除,然后使用时出错,但是不执行Destroy()又会因为addTo()出错,应该增加一种方法remove(),不移除脚本,同时又能removeFromParent()
3:[√]PlayerController过于臃肿,不利于扩展
4:[]旋转矩形的碰撞检测
5:[√]输入组件对Touch处理不好

2019-01-08
1:[]由于没有控制_onRemoved()的顺序,因此会造成引用对象不存在的情况
2:[√]需要一个状态机控制动画

2019-01-11
1:[√]子弹应该根据玩家类型不同而不同
2:[-]地图背景生成器                 --不需要生成,背景是个node,方便操作
3:[√]Boss的生命进度条,血量等控件,所以到底是一个对象还是一个控件??
4:[-]鼠标动的时候才触发人物运动,鼠标不动人物就不动,这是bug  --设定是按住屏幕才能移动,所以不是bug ...
5:[√]游戏数据持久化 -xml
6:[√]粒子循环播放与回调

2019-01-15
1:[]擦弹,决死[√]等功能的开发
2:[]弹幕子弹设计
3:[-]TaskScheduler并不能回溯之前的任务   :有回溯的必要???,用CCAction就可以了,顶多加个重复
4:[?]景深,旋涡扭曲的Shader   :部分可用CC自带特效实现
5:[√]多个同类组件获取问题            
6:[]擦除Wipe的范围圈及碰撞检测问题
7:[-]对象缓存池,对象缓存器貌似有很严重的bug,这块得规划
8:[]移动中(按键中)窗口失焦会产生bug

2019-01-20
1:[]资源的加载,场景的加载必须有一个资源的预先载入管理器(并且有回调,有进度),不然会卡住

2019-01-21
1:[√]PlayerController具有特异性,要么让键盘监听成为全局或者继承PlayerController,然后PlayerPrefab的Controller由子类添加(防止菱形继承)

2019-01-28
1:[]需要一种方法获取全局对象信息,如Boss

2019-01-30
1:[]ECS模式下UI组件移植有点难
2:[]因为UI组件不属于ECS范畴,因此entity:getChildren()/entity:getParent()可能会出错
3:[]子弹发射系统,不同子弹,特殊子弹,子弹缓存,以及出现的位置,受到碰撞后的逻辑等


--遗留bug
1:[]_onAdded()中不能使用addChild()/addTo(),会触发_onStart(),如要使用需要手动调整addScript()顺序
2:[]_onRemoved()中没有设置析构顺序,因此不能调用其他脚本(可能已经被移除了),防止出错,如需使用需要添加判断