local M = {}
local DBXManager = THSTG.ANIMATION.DBXManager

function M.loadDBXFile( ... )
    -- body
end

function M.loadPlistFile( ... )
    -- body
end


function M.createFrame()
    
end

function getAtlas(...) return DBXManager.getAtlas(...) end

return M